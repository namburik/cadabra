{
  "name": "portal-site",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "npm run build:css",
    "build:css": "npx tailwindcss -i ./assets/style.base.css -o ./assets/style.css --minify",
    "watch:css": "npx tailwindcss -i ./assets/style.base.css -o ./assets/style.css --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.13",
    "postcss": "^8.4.21",
    "tailwindcss": "^3.4.4"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
